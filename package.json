{
  "name": "@elankav/design-engine",
  "version": "0.1.0",
  "description": "Motor desacoplado de diseño del ecosistema ELANKAV.",
  "private": true,
  "type": "commonjs",
  "main": "engine/DesignEngine.js",
  "scripts": {
    "test": "node --test",
    "check": "find engine services adapters profiles config errors tests -type f -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=20"
  }
}
